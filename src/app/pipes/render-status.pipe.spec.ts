import { RenderStatusPipe } from './render-status.pipe';

describe('RenderStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
