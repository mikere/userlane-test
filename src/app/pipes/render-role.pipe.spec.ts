import { RenderRolePipe } from './render-role.pipe';

describe('RenderRolePipe', () => {
  it('create an instance', () => {
    const pipe = new RenderRolePipe();
    expect(pipe).toBeTruthy();
  });
});
